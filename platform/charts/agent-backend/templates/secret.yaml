{{/*
 Copyright (c) 2026 Seqera Labs
 All rights reserved.

 SPDX-License-Identifier: Apache-2.0

 Licensed under the Apache License, Version 2.0 (the "License");
 you may not use this file except in compliance with the License.
 You may obtain a copy of the License at

     http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing, software
 distributed under the License is distributed on an "AS IS" BASIS,
 WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 See the License for the specific language governing permissions and
 limitations under the License.
*/}}

{{- /* Don't use stringData in Secrets, as it may lead to weird glitches due to bugs in kubectl:
https://github.com/kubernetes/kubernetes/issues/89938
*/}}
{{- $commonLabels := include "common.labels.standard" (dict "customLabels" .Values.commonLabels "context" $) | fromYaml -}}
{{- $mergedlabels := include "common.tplvalues.merge" (dict "values" (list .Values.secretLabels $commonLabels) "context" .) -}}
{{- $labels := include "common.tplvalues.render" (dict "value" $mergedlabels "context" $) -}}
{{- $mergedAnnotations := include "common.tplvalues.merge" (dict "values" (list .Values.secretAnnotations .Values.commonAnnotations) "context" .) -}}
{{- $annotations := include "common.tplvalues.render" (dict "value" $mergedAnnotations "context" $) -}}
---
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "common.names.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- $labels | nindent 4 }}
  annotations: {{- $annotations | nindent 4 }}
data:
{{- if not (include "agent-backend.database.existingSecret" .) }}
  AGENT_BACKEND_DB_PASSWORD: {{ include "common.secrets.passwords.manage" (dict "secret" (include "agent-backend.database.existingSecret.secretName" .) "key" (include "agent-backend.database.existingSecret.secretKey" .) "providedValues" (list "database.password") "context" $ "honorProvidedValues" true) }}
{{- end }}
{{- if not (include "agent-backend.anthropicApiKey.existingSecret" .) }}
  ANTHROPIC_API_KEY: {{ include "common.secrets.passwords.manage" (dict "secret" (include "agent-backend.anthropicApiKey.existingSecret.secretName" .) "key" (include "agent-backend.anthropicApiKey.existingSecret.secretKey" .) "providedValues" (list "anthropicApiKey") "context" $ "honorProvidedValues" true) }}
{{- end }}
{{- if not (include "agent-backend.langchainApiKey.existingSecret" .) }}
  LANGCHAIN_API_KEY: {{ include "common.secrets.passwords.manage" (dict "secret" (include "agent-backend.langchainApiKey.existingSecret.secretName" .) "key" (include "agent-backend.langchainApiKey.existingSecret.secretKey" .) "providedValues" (list "langchainApiKey") "context" $ "honorProvidedValues" true) }}
{{- end }}
{{- if not (include "agent-backend.tokenEncrypyionKey.existingSecret" .) }}
  AGENT_BACKEND_TOKEN_ENCRYPTION_KEY: {{ include "common.secrets.passwords.manage" (dict "secret" (include "agent-backend.tokenEncrypyionKey.existingSecret.secretName" .) "key" (include "agent-backend.tokenEncrypyionKey.existingSecret.secretKey" .) "providedValues" (list "tokenEncrypyionKey") "context" $ "honorProvidedValues" true) }}
{{- end }}
{{- if not (include "agent-backend.posthogApiKey.existingSecret" .) }}
  POSTHOG_API_KEY: {{ include "common.secrets.passwords.manage" (dict "secret" (include "agent-backend.posthogApiKey.existingSecret.secretName" .) "key" (include "agent-backend.posthogApiKey.existingSecret.secretKey" .) "providedValues" (list "posthogApiKey") "context" $ "honorProvidedValues" true) }}
{{- end }}
---
{{- if .Values.global.imageCredentials }}
# Only add the imagePullSecret template if it's defined.
apiVersion: v1
kind: Secret
type: kubernetes.io/dockerconfigjson
metadata:
  name: {{ printf "%s-imagepullsecret" (include "common.names.fullname" .) }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- $labels | nindent 4 }}
  annotations: {{- $annotations | nindent 4 }}
data:
  .dockerconfigjson: {{ include "seqera.images.pullSecretCredentials" . | quote }}
{{- end }}
