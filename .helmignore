# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.helmignore
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Helm Unittest debug folder
.debug/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/

# Development values files
values_*.y*ml
values-*.y*ml
*_values.y*ml
*-values.y*ml

# Exclude tarballed charts in the root directory only
# (charts/ subdirectory .tgz files are kept for dependencies)
/*.tgz

charts/.gitkeep

.helm_ls_cache/
.claude
