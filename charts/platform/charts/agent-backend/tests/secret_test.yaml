# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
#
# Copyright (c) 2026 Seqera Labs
# All rights reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

suite: test Agent Backend secret
templates:
  - secret.yaml
chart:
  version: 1.2.3
  appVersion: "v9.9.9"
tests:
  - it: should render a Secret with default values
    set:
      database.password: test-db-password
      anthropicApiKey: test-anthropic-key
      tokenEncryptionKey: test-token-encryption-key
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: Secret
      - matchSnapshot: {}

  - it: should include AGENT_BACKEND_DB_PASSWORD when not using external secret
    set:
      database.password: mysecretpassword
    asserts:
      - isNotNull:
          path: data.AGENT_BACKEND_DB_PASSWORD

  - it: should NOT include AGENT_BACKEND_DB_PASSWORD when using external secret
    set:
      database.existingSecretName: my-external-secret
    asserts:
      - isNull:
          path: data.AGENT_BACKEND_DB_PASSWORD

  - it: should include ANTHROPIC_API_KEY when not using external secret
    set:
      anthropicApiKey: sk-ant-test123
    asserts:
      - isNotNull:
          path: data.ANTHROPIC_API_KEY

  - it: should NOT include ANTHROPIC_API_KEY when using external secret
    set:
      anthropicApiKeyExistingSecretName: my-anthropic-secret
    asserts:
      - isNull:
          path: data.ANTHROPIC_API_KEY

  - it: should include AGENT_BACKEND_TOKEN_ENCRYPTION_KEY when not using external secret
    set:
      tokenEncryptionKey: my-fernet-key
    asserts:
      - isNotNull:
          path: data.AGENT_BACKEND_TOKEN_ENCRYPTION_KEY

  - it: should NOT include AGENT_BACKEND_TOKEN_ENCRYPTION_KEY when using external secret
    set:
      tokenEncryptionKeyExistingSecretName: my-token-secret
    asserts:
      - isNull:
          path: data.AGENT_BACKEND_TOKEN_ENCRYPTION_KEY

  - it: should base64-encode user-provided tokenEncryptionKey
    set:
      tokenEncryptionKey: test-token-encryption-key
    asserts:
      - equal:
          path: data.AGENT_BACKEND_TOKEN_ENCRYPTION_KEY
          value: dGVzdC10b2tlbi1lbmNyeXB0aW9uLWtleQ==

  - it: should auto-generate a valid Fernet key when tokenEncryptionKey is empty
    asserts:
      - matchRegex:
          path: data.AGENT_BACKEND_TOKEN_ENCRYPTION_KEY
          pattern: ^[A-Za-z0-9+/=]{60}$
      - matchRegex:
          path: data.AGENT_BACKEND_TOKEN_ENCRYPTION_KEY
          pattern: ^[A-Za-z0-9_=-]{44}$
          decodeBase64: true

  - it: should NOT include AGENT_BACKEND_REDIS_PASSWORD when redis.password is not set
    asserts:
      - isNull:
          path: data.AGENT_BACKEND_REDIS_PASSWORD

  - it: should include AGENT_BACKEND_REDIS_PASSWORD when redis.password is set
    set:
      redis.password: my-redis-password
    asserts:
      - isNotNull:
          path: data.AGENT_BACKEND_REDIS_PASSWORD

  - it: should NOT include AGENT_BACKEND_REDIS_PASSWORD when using external secret
    set:
      redis.existingSecretName: my-redis-secret
    asserts:
      - isNull:
          path: data.AGENT_BACKEND_REDIS_PASSWORD

  - it: should render imagePullSecret when global.imageCredentials is set
    set:
      global.imageCredentials:
        - registry: cr.seqera.io
          username: robot
          password: secret123
    asserts:
      - hasDocuments:
          count: 2
      - isKind:
          of: Secret
        documentIndex: 1
      - equal:
          path: type
          value: kubernetes.io/dockerconfigjson
        documentIndex: 1
