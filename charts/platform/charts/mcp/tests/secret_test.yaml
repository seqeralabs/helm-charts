# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
#
# Copyright (c) 2026 Seqera Labs
# All rights reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

suite: test MCP secret
templates:
  - secret.yaml
chart:
  version: 1.2.3
  appVersion: "v9.9.9"
tests:
  - it: should render a Secret with default values
    set:
      oauth.jwtSeedString: mysecretjwtseed
      oidcToken.existingSecretName: ""
      oidcToken.tokenString: my-oidc-token
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: Secret
      - matchSnapshot: {}

  - it: should render a Secret with no values when user passes no strings and uses existing secrets
    set:
      oauth.jwtSeedSecretName: my-existing-secret
      oidcToken.existingSecretName: my-existing-oidc-secret
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: Secret
      - isEmpty:
          path: data

  - it: should include OAUTH_JWT_SECRET when not using external secret
    set:
      oauth.jwtSeedString: mysecretpassword
    asserts:
      - equal:
          path: data.OAUTH_JWT_SECRET
          value: mysecretpassword
          decodeBase64: true

  - it: should NOT include OAUTH_JWT_SECRET when using external secret
    set:
      oauth.jwtSeedSecretName: my-external-secret
    asserts:
      - notExists:
          path: data.OAUTH_JWT_SECRET

  - it: should include OAUTH_CLIENT_SECRET when using external oauth instead of default platform oauth
    set:
      micronautEnvironments: [oauth]
      oauth.clientSecretString: myclientsecret
    asserts:
      - equal:
          path: data.OAUTH_CLIENT_SECRET
          value: myclientsecret
          decodeBase64: true

  - it: should NOT include OAUTH_CLIENT_SECRET when using external secret and custom OAuth provider
    set:
      micronautEnvironments: [oauth]
      oauth.clientSecretExistingSecretName: my-client-secret
    asserts:
      - notExists:
          path: data.OAUTH_CLIENT_SECRET

  - it: should generate TOWER_OIDC_REGISTRATION_INITIAL_ACCESS_TOKEN when no existing secret is set
    set:
      oidcToken.existingSecretName: ""
    asserts:
      - matchRegex:
          path: data.TOWER_OIDC_REGISTRATION_INITIAL_ACCESS_TOKEN
          pattern: "^[A-Za-z0-9@_]{32}$"
          decodeBase64: true

  - it: should use provided OIDC token string
    set:
      oidcToken.existingSecretName: ""
      oidcToken.tokenString: my-oidc-token-value
    asserts:
      - equal:
          path: data.TOWER_OIDC_REGISTRATION_INITIAL_ACCESS_TOKEN
          value: my-oidc-token-value
          decodeBase64: true

  - it: should NOT include TOWER_OIDC_REGISTRATION_INITIAL_ACCESS_TOKEN when using existing secret
    set:
      oidcToken.existingSecretName: my-external-oidc-secret
    asserts:
      - notExists:
          path: data.TOWER_OIDC_REGISTRATION_INITIAL_ACCESS_TOKEN

  - it: should render imagePullSecret when global.imageCredentials is set
    set:
      global.imageCredentials:
        - registry: cr.seqera.io
          username: robot
          password: secret123
    asserts:
      - hasDocuments:
          count: 2
      - isKind:
          of: Secret
        documentIndex: 1
      - equal:
          path: type
          value: kubernetes.io/dockerconfigjson
        documentIndex: 1
