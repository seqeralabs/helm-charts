{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "_empty": {
      "default": "value",
      "title": "_empty",
      "type": "string"
    },
    "args": {
      "description": "Override default container args (useful when using custom images)",
      "items": {
        "required": []
      },
      "title": "args",
      "type": "array"
    },
    "command": {
      "description": "Override default container command (useful when using custom images)",
      "items": {
        "required": []
      },
      "title": "command",
      "type": "array"
    },
    "commonAnnotations": {
      "description": "Annotations to add to all deployed objects",
      "required": [],
      "title": "commonAnnotations",
      "type": "object"
    },
    "commonLabels": {
      "description": "Labels to add to all deployed objects",
      "required": [],
      "title": "commonLabels",
      "type": "object"
    },
    "configMapAnnotations": {
      "description": "Additional annotations for the ConfigMap objects. Evaluated as a template",
      "required": [],
      "title": "configMapAnnotations",
      "type": "object"
    },
    "configMapLabels": {
      "description": "Additional labels for the ConfigMap objects. Evaluated as a template",
      "required": [],
      "title": "configMapLabels",
      "type": "object"
    },
    "containerSecurityContext": {
      "description": "Configure Container Security Context.\nRef: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/#set-the-security-context-for-a-container",
      "properties": {
        "capabilities": {
          "description": "Fine-grained Linux kernel privileges to add or drop for the container",
          "properties": {
            "drop": {
              "items": {
                "anyOf": [
                  {
                    "type": "string"
                  }
                ],
                "required": []
              },
              "title": "drop",
              "type": "array"
            }
          },
          "required": [
            "drop"
          ],
          "title": "capabilities",
          "type": "object"
        },
        "enabled": {
          "default": true,
          "description": "Enable container Security Context",
          "title": "enabled",
          "type": "boolean"
        },
        "readOnlyRootFilesystem": {
          "default": true,
          "description": "Mounts the container root filesystem read-only to prevent in-place writes or tampering",
          "title": "readOnlyRootFilesystem",
          "type": "boolean"
        },
        "runAsNonRoot": {
          "default": true,
          "description": "Boolean that requires the container to run as a non-root UID (prevents starting if UID 0)",
          "title": "runAsNonRoot",
          "type": "boolean"
        },
        "runAsUser": {
          "default": 1001,
          "description": "UID the container processes run as (overrides container image default)",
          "title": "runAsUser",
          "type": "integer"
        }
      },
      "required": [
        "enabled",
        "runAsUser",
        "runAsNonRoot",
        "readOnlyRootFilesystem",
        "capabilities"
      ],
      "title": "containerSecurityContext",
      "type": "object"
    },
    "enabled": {
      "description": "Conditional property used in parent chart",
      "title": "enabled",
      "type": "boolean"
    },
    "exampleValue": {
      "default": "common-chart",
      "description": "# bitnami/common\n# It is required by CI/CD tools and processes.\n#\n#",
      "title": "exampleValue",
      "type": "string"
    },
    "extraDeploy": {
      "description": "Array of extra objects to deploy with the release",
      "items": {
        "required": []
      },
      "title": "extraDeploy",
      "type": "array"
    },
    "extraEnvVars": {
      "description": "Extra environment variables",
      "items": {
        "required": []
      },
      "title": "extraEnvVars",
      "type": "array"
    },
    "extraEnvVarsCMs": {
      "description": "List of ConfigMaps containing extra env vars",
      "items": {
        "required": []
      },
      "title": "extraEnvVarsCMs",
      "type": "array"
    },
    "extraEnvVarsSecrets": {
      "description": "List of Secrets containing extra env vars",
      "items": {
        "required": []
      },
      "title": "extraEnvVarsSecrets",
      "type": "array"
    },
    "extraOptionsSpec": {
      "description": "Extra options to place under .spec (e.g. replicas, strategy, revisionHistoryLimit, etc).\nEvaluated as a template",
      "required": [],
      "title": "extraOptionsSpec",
      "type": "object"
    },
    "extraOptionsTemplateSpec": {
      "description": "Extra options to place under .spec.template.spec (e.g. nodeSelector, affinity, restartPolicy,\netc). Evaluated as a template",
      "required": [],
      "title": "extraOptionsTemplateSpec",
      "type": "object"
    },
    "extraVolumeMounts": {
      "description": "List of volume mounts to add to the container (evaluated as template). Normally used with\n`extraVolumes`",
      "items": {
        "required": []
      },
      "title": "extraVolumeMounts",
      "type": "array"
    },
    "extraVolumes": {
      "description": "List of volumes to add to the deployment (evaluated as template). Requires setting\n`extraVolumeMounts`",
      "items": {
        "required": []
      },
      "title": "extraVolumes",
      "type": "array"
    },
    "global": {
      "description": "Values in the .global section can be accessed by both parent and child subcharts as\n.Values.global.*: https://helm.sh/docs/chart_template_guide/subcharts_and_globals/",
      "properties": {
        "agentBackendDomain": {
          "default": "{{ printf \"ai-api.%s\" .Values.global.platformExternalDomain }}",
          "description": "Domain where the Agent Backend service listens. Evaluated as a template",
          "title": "agentBackendDomain",
          "type": "string"
        },
        "imageCredentials": {
          "description": "Optional credentials to log in and fetch images from a private registry. These credentials\nare shared with all the subcharts automatically",
          "items": {
            "required": []
          },
          "title": "imageCredentials",
          "type": "array"
        },
        "imageCredentialsSecrets": {
          "description": "Optional list of existing Secrets containing image pull credentials to use for pulling\nimages from private registries. These Secrets are shared with all the subcharts automatically",
          "items": {
            "required": []
          },
          "title": "imageCredentialsSecrets",
          "type": "array"
        },
        "ingress": {
          "description": "Ingress defaults shared across the parent chart and all subcharts. Each subchart's local\n`ingress.*` value takes precedence when set; otherwise the global is used.",
          "properties": {
            "annotations": {
              "description": "Annotations merged into the Ingress. Local `ingress.annotations` wins on key collision.\nEvaluated as a template",
              "required": [],
              "title": "annotations",
              "type": "object"
            },
            "defaultPathType": {
              "default": "Prefix",
              "description": "Default path type applied to ingress rules when `ingress.defaultPathType` is not set.\n`Prefix` works for nginx, traefik, AWS ALB, and most modern controllers.",
              "title": "defaultPathType",
              "type": "string"
            },
            "enabled": {
              "default": false,
              "description": "Enable Ingress for this chart. OR'd with the chart's local `ingress.enabled` so\nsetting this once at the parent enables all subchart Ingresses.",
              "title": "enabled",
              "type": "boolean"
            },
            "extraLabels": {
              "description": "Extra labels merged into the Ingress. Local `ingress.extraLabels` wins on key collision.\nEvaluated as a template",
              "required": [],
              "title": "extraLabels",
              "type": "object"
            },
            "ingressClassName": {
              "default": "",
              "description": "Default ingress class name applied when `ingress.ingressClassName` is not set",
              "title": "ingressClassName",
              "type": "string"
            },
            "path": {
              "default": "/",
              "description": "Default path applied to ingress rules when `ingress.path` is not set.\nAWS ALB users should override to `/*`.",
              "title": "path",
              "type": "string"
            },
            "tls": {
              "description": "TLS entries concatenated with the local `ingress.tls`. Evaluated as a template",
              "items": {
                "required": []
              },
              "title": "tls",
              "type": "array"
            }
          },
          "required": [
            "enabled",
            "path",
            "defaultPathType",
            "ingressClassName",
            "annotations",
            "extraLabels",
            "tls"
          ],
          "title": "ingress",
          "type": "object"
        },
        "platformExternalDomain": {
          "default": "example.com",
          "description": "Domain where Seqera Platform listens",
          "title": "platformExternalDomain",
          "type": "string"
        },
        "platformServiceAddress": {
          "default": "",
          "description": "Seqera Platform Service name: can be the internal Kubernetes hostname or an external ingress\nhostname. Evaluated as a template.\nRequired when deploying this subchart standalone. When deploying as part of the parent\n`platform` umbrella chart, this value is inherited from the parent chart's `global` section",
          "title": "platformServiceAddress",
          "type": "string"
        },
        "platformServicePort": {
          "default": "",
          "description": "(int) Seqera Platform Service port.\nRequired when deploying this subchart standalone. When deploying as part of the parent\n`platform` umbrella chart, this value is inherited from the parent chart's `global` section",
          "required": [],
          "title": "platformServicePort",
          "type": [
            "integer",
            "string",
            "null"
          ]
        },
        "portalWebDomain": {
          "default": "{{ printf \"ai.%s\" .Values.global.platformExternalDomain }}",
          "description": "Domain where the Portal Web frontend listens. Evaluated as a template",
          "title": "portalWebDomain",
          "type": "string"
        }
      },
      "required": [
        "platformExternalDomain",
        "platformServiceAddress",
        "agentBackendDomain",
        "portalWebDomain",
        "ingress",
        "imageCredentials",
        "imageCredentialsSecrets"
      ],
      "title": "global",
      "type": "object"
    },
    "image": {
      "properties": {
        "digest": {
          "default": "",
          "description": "Container image digest in the format `sha256:1234abcdef`",
          "title": "digest",
          "type": "string"
        },
        "pullPolicy": {
          "default": "IfNotPresent",
          "description": "imagePullPolicy for the container\nRef: https://kubernetes.io/docs/concepts/containers/images/#pre-pulled-images",
          "title": "pullPolicy",
          "type": "string"
        },
        "pullSecrets": {
          "description": "List of imagePullSecrets\nSecrets must be created in the same namespace, for example using the .extraDeploy array\nRef: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/",
          "items": {
            "required": []
          },
          "title": "pullSecrets",
          "type": "array"
        },
        "registry": {
          "default": "",
          "description": "Container image registry",
          "title": "registry",
          "type": "string"
        },
        "repository": {
          "default": "ai/portal/web",
          "description": "Container image repository",
          "title": "repository",
          "type": "string"
        },
        "tag": {
          "default": "",
          "description": "Container image tag. Defaults to the chart's appVersion (the `appVersion` field in\nChart.yaml)",
          "required": [],
          "title": "tag",
          "type": [
            "string",
            "number"
          ]
        }
      },
      "required": [
        "registry",
        "repository",
        "digest",
        "pullPolicy",
        "pullSecrets"
      ],
      "title": "image",
      "type": "object"
    },
    "ingress": {
      "properties": {
        "annotations": {
          "description": "Ingress annotations specific to your load balancer. Evaluated as a template",
          "required": [],
          "title": "annotations",
          "type": "object"
        },
        "defaultBackend": {
          "description": "Configure the default service for the ingress (evaluated as template)\nImportant: make sure only one defaultBackend is defined across the k8s cluster: if the\ningress doesn't reconcile successfully, 'describe ingress \u003cname\u003e' will report problems",
          "required": [],
          "title": "defaultBackend",
          "type": "object"
        },
        "defaultPathType": {
          "default": "",
          "description": "Default path type for the Ingress. When empty, falls back to `global.ingress.defaultPathType`",
          "title": "defaultPathType",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Enable ingress for Portal Web",
          "title": "enabled",
          "type": "boolean"
        },
        "extraHosts": {
          "description": "Additional hosts you want to include. Evaluated as a template.\nEach path's backend takes either `portNumber` or `portName` (e.g. `use-annotation`\nto reference an ALB action defined via `ingress.annotations`).",
          "items": {
            "required": []
          },
          "title": "extraHosts",
          "type": "array"
        },
        "extraLabels": {
          "description": "Additional labels for the ingress object. Evaluated as a template",
          "required": [],
          "title": "extraLabels",
          "type": "object"
        },
        "ingressClassName": {
          "default": "",
          "description": "Name of the ingress class (replaces the deprecated annotation `kubernetes.io/ingress.class`).\nWhen empty, falls back to `global.ingress.ingressClassName`",
          "title": "ingressClassName",
          "type": "string"
        },
        "path": {
          "default": "",
          "description": "Path for the main ingress rule. When empty, falls back to `global.ingress.path`",
          "title": "path",
          "type": "string"
        },
        "tls": {
          "description": "TLS configuration. Evaluated as a template",
          "items": {
            "required": []
          },
          "title": "tls",
          "type": "array"
        }
      },
      "required": [
        "enabled",
        "path",
        "defaultPathType",
        "defaultBackend",
        "extraHosts",
        "annotations",
        "extraLabels",
        "ingressClassName",
        "tls"
      ],
      "title": "ingress",
      "type": "object"
    },
    "initContainers": {
      "description": "Additional init containers for the pod. Evaluated as a template",
      "items": {
        "required": []
      },
      "title": "initContainers",
      "type": "array"
    },
    "livenessProbe": {
      "description": "Configure extra options for the liveness probe",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Enable liveness probe",
          "title": "enabled",
          "type": "boolean"
        },
        "failureThreshold": {
          "default": 3,
          "description": "Consecutive failures before restarting the container",
          "title": "failureThreshold",
          "type": "integer"
        },
        "httpGet": {
          "properties": {
            "path": {
              "default": "/",
              "description": "HTTP GET path for liveness probe",
              "title": "path",
              "type": "string"
            },
            "port": {
              "default": "{{ .Values.service.http.targetPort }}",
              "description": "HTTP GET port for liveness probe. Evaluated as a template",
              "title": "port",
              "type": "string"
            }
          },
          "required": [
            "path",
            "port"
          ],
          "title": "httpGet",
          "type": "object"
        },
        "initialDelaySeconds": {
          "default": 30,
          "description": "Delay before first check",
          "title": "initialDelaySeconds",
          "type": "integer"
        },
        "periodSeconds": {
          "default": 10,
          "description": "Regular check interval during normal operation",
          "title": "periodSeconds",
          "type": "integer"
        },
        "successThreshold": {
          "default": 1,
          "description": "Typically 1 (usually ignored)",
          "title": "successThreshold",
          "type": "integer"
        },
        "timeoutSeconds": {
          "default": 5,
          "description": "Short timeout to detect hung containers quickly",
          "title": "timeoutSeconds",
          "type": "integer"
        }
      },
      "required": [
        "enabled",
        "httpGet",
        "initialDelaySeconds",
        "periodSeconds",
        "timeoutSeconds",
        "failureThreshold",
        "successThreshold"
      ],
      "title": "livenessProbe",
      "type": "object"
    },
    "nextjsCacheSizeLimit": {
      "default": "500Mi",
      "description": "Size limit for the /app/.next/cache emptyDir volume",
      "title": "nextjsCacheSizeLimit",
      "type": "string"
    },
    "podAnnotations": {
      "description": "Additional annotations for the pod. Evaluated as a template",
      "required": [],
      "title": "podAnnotations",
      "type": "object"
    },
    "podLabels": {
      "description": "Additional labels for the pod. Evaluated as a template",
      "required": [],
      "title": "podLabels",
      "type": "object"
    },
    "podSecurityContext": {
      "description": "Configure Pods Security Context.\nRef: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/#set-the-security-context-for-a-pod\nEmpty map to disable Pod Security Context configuration",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Enable pod Security Context",
          "title": "enabled",
          "type": "boolean"
        },
        "fsGroup": {
          "default": 1001,
          "description": "Sets the GID that Kubernetes will apply to mounted volumes and created files so processes\nin the pod can share group-owned access",
          "title": "fsGroup",
          "type": "integer"
        }
      },
      "required": [
        "enabled",
        "fsGroup"
      ],
      "title": "podSecurityContext",
      "type": "object"
    },
    "readinessProbe": {
      "description": "Configure extra options for the readiness probe",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Enable readiness probe",
          "title": "enabled",
          "type": "boolean"
        },
        "failureThreshold": {
          "default": 3,
          "description": "Consecutive failures before marking the container Unready (no restart)",
          "title": "failureThreshold",
          "type": "integer"
        },
        "httpGet": {
          "properties": {
            "path": {
              "default": "/",
              "description": "HTTP GET path for readiness probe",
              "title": "path",
              "type": "string"
            },
            "port": {
              "default": "{{ .Values.service.http.targetPort }}",
              "description": "HTTP GET port for readiness probe. Evaluated as a template",
              "title": "port",
              "type": "string"
            }
          },
          "required": [
            "path",
            "port"
          ],
          "title": "httpGet",
          "type": "object"
        },
        "initialDelaySeconds": {
          "default": 10,
          "description": "Delay before first check",
          "title": "initialDelaySeconds",
          "type": "integer"
        },
        "periodSeconds": {
          "default": 5,
          "description": "Regular check interval during normal operation",
          "title": "periodSeconds",
          "type": "integer"
        },
        "successThreshold": {
          "default": 1,
          "description": "Number of consecutive successes required to mark the container Ready after failures",
          "title": "successThreshold",
          "type": "integer"
        },
        "timeoutSeconds": {
          "default": 3,
          "description": "Short timeout to detect unresponsive containers for readiness",
          "title": "timeoutSeconds",
          "type": "integer"
        }
      },
      "required": [
        "enabled",
        "httpGet",
        "initialDelaySeconds",
        "periodSeconds",
        "timeoutSeconds",
        "failureThreshold",
        "successThreshold"
      ],
      "title": "readinessProbe",
      "type": "object"
    },
    "resources": {
      "description": "Container requests and limits for different resources like CPU or memory",
      "properties": {
        "limits": {
          "properties": {
            "memory": {
              "default": "500Mi",
              "title": "memory",
              "type": "string"
            }
          },
          "required": [
            "memory"
          ],
          "title": "limits",
          "type": "object"
        },
        "requests": {
          "properties": {
            "cpu": {
              "default": "100m",
              "title": "cpu",
              "type": "string"
            },
            "memory": {
              "default": "500Mi",
              "title": "memory",
              "type": "string"
            }
          },
          "required": [
            "cpu",
            "memory"
          ],
          "title": "requests",
          "type": "object"
        }
      },
      "required": [
        "requests",
        "limits"
      ],
      "title": "resources",
      "type": "object"
    },
    "secretAnnotations": {
      "description": "Additional annotations for the Secret objects. Evaluated as a template",
      "required": [],
      "title": "secretAnnotations",
      "type": "object"
    },
    "secretLabels": {
      "description": "Additional labels for the Secret objects. Evaluated as a template",
      "required": [],
      "title": "secretLabels",
      "type": "object"
    },
    "service": {
      "properties": {
        "extraOptions": {
          "description": "Extra Service options to place under .spec (for example, clusterIP, loadBalancerIP,\nexternalTrafficPolicy, externalIPs). Evaluated as a template",
          "required": [],
          "title": "extraOptions",
          "type": "object"
        },
        "extraServices": {
          "description": "Other services that should live in the Service object\nhttps://kubernetes.io/docs/concepts/services-networking/service/#defining-a-service",
          "items": {
            "required": []
          },
          "title": "extraServices",
          "type": "array"
        },
        "http": {
          "properties": {
            "name": {
              "default": "http",
              "description": "Service name to use",
              "title": "name",
              "type": "string"
            },
            "nodePort": {
              "default": "null",
              "description": "Service node port, only used when service.type is Nodeport or LoadBalancer\nChoose port between 30000-32767, unless the cluster was configured differently than default",
              "required": [],
              "title": "nodePort",
              "type": [
                "integer",
                "null"
              ]
            },
            "port": {
              "default": 80,
              "description": "Service port",
              "required": [],
              "title": "port",
              "type": [
                "integer",
                "string"
              ]
            },
            "targetPort": {
              "default": 3000,
              "description": "Port on the pod/container that the Service forwards traffic to (can be a number or\nnamed port, distinct from the Service's external port)",
              "title": "targetPort",
              "type": "integer"
            }
          },
          "required": [
            "name",
            "targetPort"
          ],
          "title": "http",
          "type": "object"
        },
        "type": {
          "default": "ClusterIP",
          "description": "Service type.\nNote: ingresses using AWS ALB require the service to be NodePort",
          "title": "type",
          "type": "string"
        }
      },
      "required": [
        "type",
        "http",
        "extraServices",
        "extraOptions"
      ],
      "title": "service",
      "type": "object"
    },
    "serviceAccount": {
      "description": "Service account configuration",
      "properties": {
        "annotations": {
          "description": "Service account annotations",
          "required": [],
          "title": "annotations",
          "type": "object"
        },
        "automountServiceAccountToken": {
          "default": false,
          "description": "Automatically mount service account token",
          "required": [],
          "title": "automountServiceAccountToken",
          "type": [
            "boolean",
            "string"
          ]
        },
        "create": {
          "default": true,
          "description": "Whether to create a ServiceAccount. When `true`, the ServiceAccount named by\n`serviceAccount.name` is created (a `\u003crelease\u003e-\u003cchart\u003e-sa` name is generated when it is unset).\nSet to `false` to use an existing, externally-managed ServiceAccount referenced by\n`serviceAccount.name`.",
          "title": "create",
          "type": "boolean"
        },
        "imagePullSecretNames": {
          "description": "Names of Secrets containing credentials to pull images from registries",
          "items": {
            "required": []
          },
          "title": "imagePullSecretNames",
          "type": "array"
        },
        "name": {
          "default": "",
          "description": "Name of the ServiceAccount used by this chart's workloads. When empty, a name is generated\nfrom the release name. Applies whether the ServiceAccount is created by this chart\n(`create: true`) or managed externally (`create: false`).",
          "title": "name",
          "type": "string"
        }
      },
      "required": [
        "create",
        "name",
        "annotations",
        "imagePullSecretNames"
      ],
      "title": "serviceAccount",
      "type": "object"
    },
    "startupProbe": {
      "description": "Configure extra options for the startup probe",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable startup probe",
          "title": "enabled",
          "type": "boolean"
        },
        "failureThreshold": {
          "default": 5,
          "description": "Consecutive failures during startup before killing the container (instead of immediate\nrestarts)",
          "title": "failureThreshold",
          "type": "integer"
        },
        "httpGet": {
          "properties": {
            "path": {
              "default": "/",
              "description": "HTTP GET path for startup probe",
              "title": "path",
              "type": "string"
            },
            "port": {
              "default": "{{ .Values.service.http.targetPort }}",
              "description": "HTTP GET port for startup probe. Evaluated as a template",
              "title": "port",
              "type": "string"
            }
          },
          "required": [
            "path",
            "port"
          ],
          "title": "httpGet",
          "type": "object"
        },
        "initialDelaySeconds": {
          "default": 5,
          "description": "Longer initial wait to accommodate slow-starting apps",
          "title": "initialDelaySeconds",
          "type": "integer"
        },
        "periodSeconds": {
          "default": 10,
          "description": "Often set longer to avoid frequent checks while starting",
          "title": "periodSeconds",
          "type": "integer"
        },
        "successThreshold": {
          "default": 1,
          "description": "Number of consecutive successes required to consider startup complete and enable\nliveness/readiness",
          "title": "successThreshold",
          "type": "integer"
        },
        "timeoutSeconds": {
          "default": 3,
          "description": "Can be longer to allow slow initialization responses",
          "title": "timeoutSeconds",
          "type": "integer"
        }
      },
      "required": [
        "enabled",
        "httpGet",
        "initialDelaySeconds",
        "periodSeconds",
        "timeoutSeconds",
        "failureThreshold",
        "successThreshold"
      ],
      "title": "startupProbe",
      "type": "object"
    },
    "tmpDirSizeLimit": {
      "default": "500Mi",
      "description": "Size limit for the /tmp emptyDir volume",
      "title": "tmpDirSizeLimit",
      "type": "string"
    }
  },
  "required": [
    "global",
    "image",
    "service",
    "tmpDirSizeLimit",
    "nextjsCacheSizeLimit",
    "initContainers",
    "command",
    "args",
    "podLabels",
    "podAnnotations",
    "extraOptionsSpec",
    "extraOptionsTemplateSpec",
    "extraEnvVars",
    "extraEnvVarsCMs",
    "extraEnvVarsSecrets",
    "extraVolumes",
    "extraVolumeMounts",
    "podSecurityContext",
    "containerSecurityContext",
    "resources",
    "startupProbe",
    "readinessProbe",
    "livenessProbe",
    "serviceAccount",
    "ingress",
    "extraDeploy",
    "commonAnnotations",
    "commonLabels",
    "secretLabels",
    "secretAnnotations",
    "configMapLabels",
    "configMapAnnotations"
  ],
  "type": "object"
}
